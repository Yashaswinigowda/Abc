/*Reverse of a number*/

  function fun(n) {
  if(n===0)
    return 0;
  else if(n>0){
  var m = n+'';
  return m.split('').reverse().join('');
  }
  else{
    var m = Math.abs(n)+'';
    return - + m.split('').reverse().join('');
  }
 }  
